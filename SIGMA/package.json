{
  "name": "edurede-integrada",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de gestao escolar integrada com Firebase Hosting, Firestore e GitHub Actions.",
  "type": "module",
  "scripts": {
    "start": "node serve-local.mjs",
    "check": "node --check app.js && node --check firebase-service.js && node --check serve-local.mjs",
    "deploy": "firebase deploy --only hosting,firestore:rules,database"
  },
  "devDependencies": {
    "firebase-tools": "^14.2.1"
  }
}
